version: 2.1

jobs:
  build:
    docker:
      - image: returntocorp/sgrep:develop
    working_directory: /home/repo
    steps:
      - run: "apk install git"
      - checkout
      - run: ls -la
      - run:
          name: "Run sgrep"
          command: sgrep-lint --config=https://sgrep.live/c/r/r2c.python; 