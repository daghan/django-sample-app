version: 2.1

jobs:
  build:
    docker:
      - image: returntocorp/sgrep:develop
    working_directory: /home/repo
    steps:
      - checkout
      - run:
          name: "Run sgrep"
          command: sgrep-lint --error --json --config=https://sgrep.live/c/r/r2c.python 1>&2; echo "done"
